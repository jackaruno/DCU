#include <stdio.h>
#include <stdlib.h>

int largest(nums);
int smallest(nums);


void find_num(char *arg[50]){
    char *type = arg[1]
    if (type=="largest"){
        return largest(arg[])
    } else {

    }
}



int main(int argc, char *argv[]){
    find_num(argv);
    return 0;
}